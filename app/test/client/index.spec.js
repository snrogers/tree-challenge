describe('A Thing', function() {
  // let lineItem;
  // beforeEach(function() { lineItem = new UnitLineItemVM(unitNewJson) });

  it('does things', function() { throw new Error('It didnt do a thing'); });

});
